# Frontend environment variables (Vite)
# These are public values bundled into the client. Do NOT place secrets here.
# Use your hosting platform to inject production values.

# Base API URL for the backend (no trailing slash)
VITE_API_URL=http://localhost:5000

